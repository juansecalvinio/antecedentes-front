import styled from 'styled-components';

export const StyledHeader = styled.header`
    align-items: center;
    display: flex;
    background: transparent;
    height: 80px;
    justify-content: space-between;
    margin: 1em;
`;

export const StyledLogo = styled.img``; // Lo dejo vacio para futuros cambios